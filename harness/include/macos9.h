/* harness stub for the macos9 frontend umbrella header — NO Carbon.
 * Only what the JS glue + reconvert reference. __MACOS9__ stays UNdefined
 * so the Toolbox #ifdef blocks compile out. */
#ifndef MACOS9_H
#define MACOS9_H
#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#endif
