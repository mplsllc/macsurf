# Private archive folder (intermediate ship zips, dev snapshots, etc.)
.private/

# Per-user assistant state, prompts, and project-context briefs — local
# to the working copy and not published in the repo.
.claude/
.gemini/
CLAUDE.md
GEMINI.md
AGENTS.md
COPILOT.md

# Build archives & fix tars (these go to .private/archives/)
*.zip
*.tar
*.tar.gz
*.tgz
*.sit
*.hqx
# Exceptions: the public build pack + ready-to-run binary live in builds/
!builds/MacSurf-BuildPack.sit
!builds/MacSurf.sit

# Build artifacts
MacSurf
MacSurf.xml
*.o
*.a
*.lib

# Runtime logs
*.log
MacSurf Debug.log
StdLog

# macOS resource-fork metadata
._*
.DS_Store

# Scratch error logs
errors*.txt
errrors*.txt
newerrors*.txt
newerrors*txt
build-error-*.txt

# Node / Gemini CLI dependencies (moved to .private)
node_modules/
package.json
package-lock.json

# Legacy / scratch build trees
clean_macsurf/
flat_macsurf_sources/
tree_macsurf/
build/
for-gemini/

# Misc scratch
text.txt
test_ending.txt
test_pragma.c
animation.as
forgem/

# macSSL is its own git repo at macSSL/ — manage separately (submodule or merge later)
macSSL/
