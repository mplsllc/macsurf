/* * MacSurf - main.c * Entry point, Toolbox init, OT lifecycle, WaitNextEvent loop, event dispatch. * * Open Transport is initialized once at startup and closed once at exit, * matching the Retro68 ot-tcp-demo.c pattern (verified on Mac OS 9.2 hw). * Per-fetch code in macos9_fetch.c only opens and closes individual endpoints. * * The cooperative event loop: WaitNextEvent -> dispatch -> schedule_run -> loop. * If a fetch is queued via gui_window::fetch_pending, the loop runs it * synchronously in between WaitNextEvent calls. OT yields happen through the * OT sync-idle notifier, so other cooperative threads keep running during * blocking OT calls even though the fetch is "synchronous" from our view. */#include <stdlib.h>#include <string.h>#include <stdio.h>#include "utils/utils.h"#include "utils/nsoption.h"#include "utils/log.h"#include "utils/messages.h"#include "utils/nsurl.h"#include "netsurf/netsurf.h"#include "netsurf/bitmap.h"#include "netsurf/browser_window.h"#include "netsurf/plotters.h"#include "netsurf/types.h"#include "macos9/macos9.h"extern const struct plotter_table macos9_plotters;#ifdef __MACOS9__#include <Events.h>#include <MacWindows.h>#include <MacMemory.h>#include <Menus.h>#include <Fonts.h>#include <Dialogs.h>#include <Quickdraw.h>#include <Devices.h>#include <ToolUtils.h>#include <Controls.h>#include <Gestalt.h>#include <Appearance.h>#include <Sound.h>#include <OpenTransport.h>#include <OpenTptInternet.h>#include <Threads.h>#ifdef WITH_DUKTAPE#include "javascript/macsurf_js.h"#include "content/handlers/javascript/js.h"#endif/* CW8's Controls.h lacks these Appearance Manager part codes. */#ifndef kControlUpButtonPart#define kControlUpButtonPart    20#endif#ifndef kControlDownButtonPart#define kControlDownButtonPart  21#endif#ifndef kControlPageUpPart#define kControlPageUpPart      22#endif#ifndef kControlPageDownPart#define kControlPageDownPart    23#endif#ifndef kControlIndicatorPart#define kControlIndicatorPart   129#endif/* CW8's Carbon init constant is also missing. */#ifndef kInitOTForApplicationMask#define kInitOTForApplicationMask 0x00000002#endif/* CW8 expands New/DisposeControlActionUPP into NewRoutineDescriptor / * DisposeRoutineDescriptor calls, which are not in CarbonLib. Under Carbon * UPPs are just native function pointers, so override the macros. */#ifdef NewControlActionUPP#undef NewControlActionUPP#endif#ifdef DisposeControlActionUPP#undef DisposeControlActionUPP#endif#define NewControlActionUPP(proc)    ((ControlActionUPP)(proc))#define DisposeControlActionUPP(upp) ((void)(upp))#else/* Linux build stubs for syntax checking. */typedef unsigned short EventKind;typedef unsigned long UInt32;typedef short SInt16;#ifndef __MACTYPES__typedef struct { SInt16 v; SInt16 h; } Point;#endiftypedef struct {	EventKind what;	UInt32 message;	UInt32 when;	Point where;	unsigned short modifiers;} EventRecord;enum { everyEvent=~0, nullEvent=0, mouseDown=1, mouseUp=2, keyDown=3,	keyUp=4, autoKey=5, updateEvt=6, diskEvt=7, activateEvt=8, osEvt=15 };enum { cmdKey=0x0100, shiftKey=0x0200 };enum { inMenuBar=1, inContent=3, inDrag=4, inGrow=5, inGoAway=6 };#ifndef __MACTYPES__typedef int Boolean;#endiftypedef void *MenuHandle;typedef void *GrafPtr;enum { kControlUpButtonPart=20, kControlDownButtonPart=21,	kControlPageUpPart=22, kControlPageDownPart=23,	kControlIndicatorPart=129 };static struct { void *thePort; struct { Rect bounds; } screenBits; } qd;static void MaxApplZone(void) {}static void MoreMasters(void) {}static void InitCursor(void) {}static void FlushEvents(unsigned short m, unsigned short s) { (void)m; (void)s; }static MenuHandle NewMenu(short id, const unsigned char *t) { (void)id;(void)t; return NULL; }static void AppendMenu(MenuHandle m, const unsigned char *d) { (void)m;(void)d; }static void AppendResMenu(MenuHandle m, unsigned long t) { (void)m;(void)t; }static void InsertMenu(MenuHandle m, short b) { (void)m;(void)b; }static void DrawMenuBar(void) {}static long MenuSelect(Point p) { (void)p; return 0; }static long MenuKey(short c) { (void)c; return 0; }static void HiliteMenu(short id) { (void)id; }static short FindWindow(Point p, WindowRef *w) { (void)p; *w=NULL; return 0; }#define PSTR(s) ((const unsigned char *)(s))static Boolean WaitNextEvent(unsigned short m, EventRecord *e, UInt32 s, void *r){ (void)m;(void)s;(void)r; e->what=nullEvent; return 0; }static void BeginUpdate(WindowRef w) { (void)w; }static void EndUpdate(WindowRef w) { (void)w; }static void EraseRect(const Rect *r) { (void)r; }static void FrameRect(const Rect *r) { (void)r; }static void InvertRect(const Rect *r) { (void)r; }static void MoveTo(short h, short v) { (void)h;(void)v; }static void DrawString(const unsigned char *s) { (void)s; }static void DrawText(const void *b, short s, short l) { (void)b;(void)s;(void)l; }static void TextFont(short f) { (void)f; }static void TextSize(short s) { (void)s; }static void TextFace(short f) { (void)f; }static void SetPort(GrafPtr p) { (void)p; }static void GetPort(GrafPtr *p) { (void)p; }static GrafPtr GetWindowPort(WindowRef w) { (void)w; return NULL; }static void SetPortWindowPort(WindowRef w) { (void)w; }static void GetWindowPortBounds(WindowRef w, Rect *r){ (void)w; r->top=0;r->left=0;r->bottom=480;r->right=640; }static void InvalWindowRect(WindowRef w, const Rect *r) { (void)w;(void)r; }static void DragWindow(WindowRef w, Point p, const Rect *b) { (void)w;(void)p;(void)b; }static long GrowWindow(WindowRef w, Point p, const Rect *l){ (void)w;(void)p;(void)l; return 0; }static void SizeWindow(WindowRef w, short h, short v, Boolean u){ (void)w;(void)h;(void)v;(void)u; }static void SetRect(Rect *r, short l, short t, short rt, short b){ r->top=t; r->left=l; r->bottom=b; r->right=rt; }static WindowRef GetNextWindow(WindowRef w) { (void)w; return NULL; }typedef short ControlPartCode;typedef void (*ControlActionProcPtr)(ControlRef, ControlPartCode);typedef ControlActionProcPtr ControlActionUPP;static ControlActionUPP NewControlActionUPP(ControlActionProcPtr p) { return p; }static void DisposeControlActionUPP(ControlActionUPP u) { (void)u; }static WindowRef GetControlOwner(ControlRef c) { (void)c; return NULL; }#define kFontIDMonaco 4#define kFontIDGeneva 3#define pascalstatic Boolean TrackGoAway(WindowRef w, Point p) { (void)w;(void)p; return 0; }static void SelectWindow(WindowRef w) { (void)w; }static WindowRef FrontWindow(void) { return NULL; }static void DrawControls(WindowRef w) { (void)w; }static short FindControl(Point p, WindowRef w, ControlRef *c){ (void)p;(void)w; *c=NULL; return 0; }static short TrackControl(ControlRef c, Point p, void *a) { (void)c;(void)p;(void)a; return 0; }static long GetWRefCon(WindowRef w) { (void)w; return 0; }static short GetControlValue(ControlRef c) { (void)c; return 0; }static short GetControlMaximum(ControlRef c) { (void)c; return 0; }static void SetControlValue(ControlRef c, short v) { (void)c;(void)v; }static void GlobalToLocal(Point *p) { (void)p; }static int InitOpenTransport(void) { return 0; }static void CloseOpenTransport(void) {}static void SysBeep(short d) { (void)d; }static int Gestalt(unsigned long sel, long *r) { (void)sel; *r=0; return 0; }static void RegisterAppearanceClient(void) {}#define gestaltOpenTpt 0#define gestaltAppearanceAttr 0#define noErr 0#endifbool macos9_done = false;bool macos9_fetching = false;extern struct gui_window_table *macos9_window_table;extern struct gui_bitmap_table *macos9_bitmap_table;extern struct gui_layout_table *macos9_layout_table;extern struct gui_fetch_table macos9_fetch_table;extern struct gui_misc_table macos9_misc_table;extern struct gui_clipboard_table *macos9_clipboard_table;extern struct gui_utf8_table *macos9_utf8_table;extern struct gui_download_table *macos9_download_table;extern const struct plotter_table macos9_plotters;static bool ot_initialized = false;#ifdef __MACOS9__OTClientContextPtr macos9_ot_context = NULL;#endif/* * Fetch callback - unpack response into gui_window, strip HTML, word-wrap, * and invalidate the content area for redraw. Public because macos9_fetch_url * takes a function pointer. */voidmacos9_fetch_callback(struct gui_window *gw,		const char *data, long len, int status){	if (gw == NULL) return;	if (status < 0 || data == NULL || len <= 0) {		gw->content_len = 0;		if (data != NULL && len > 0) {			long copy = len;			if (copy > MACSURF_CONTENT_MAX - 1) copy = MACSURF_CONTENT_MAX - 1;			memcpy(gw->content, data, copy);			gw->content[copy] = '\0';			gw->content_len = copy;		} else {			gw->content_len = (long)sprintf(gw->content,					"Fetch error (status %d)", status);		}		strncpy(gw->status, "Error", sizeof(gw->status) - 1);		gw->status[sizeof(gw->status) - 1] = '\0';	} else {		gw->content_len = macos9_strip_html(data, len,				gw->content, MACSURF_CONTENT_MAX);#ifdef WITH_DUKTAPE		/* Stage-7 demo path: extract <script> blocks from the		 * fetched HTML, run them via Duktape, append console.log		 * output to the displayed content. */		{			jsheap *script_heap = NULL;			macsurf_js_console_reset();			if (js_newheap(0, &script_heap) == NSERROR_OK &&			    script_heap != NULL) {				int nscripts;				nscripts = macsurf_js_run_scripts_in_html(					(struct jscontext *)script_heap,					data, len);				if (nscripts > 0) {					const char *out;					long room;					long olen;					const char banner[] =						"\n\n=== JS OUTPUT ===\n";					long blen = (long)sizeof(banner) - 1;					room = MACSURF_CONTENT_MAX - 1 -						gw->content_len;					if (room > blen) {						memcpy(gw->content+gw->content_len,							banner, blen);						gw->content_len += blen;						room -= blen;					}					out = macsurf_js_console_get();					olen = (long)strlen(out);					if (olen > room) olen = room;					if (olen > 0) {						memcpy(gw->content+gw->content_len,							out, olen);						gw->content_len += olen;					}					gw->content[gw->content_len] = 0;				}				js_destroyheap(script_heap);			}		}#endif		strncpy(gw->status, "Ready", sizeof(gw->status) - 1);		gw->status[sizeof(gw->status) - 1] = '\0';	}	gw->top_line = 0;	macos9_window_rewrap(gw);	macos9_window_update_scrollbar(gw);	macos9_window_invalidate_all(gw);}/* ---- Menu bar ---- */static voidmacos9_handle_menu(short menu_id, short item){	switch (menu_id) {	case MENU_APPLE:		break;	case MENU_FILE:		switch (item) {		case ITEM_FILE_NEW:			{				struct gui_window *nw = macos9_create_initial_window();				if (nw != NULL) macos9_window_home(nw);			}			break;		case ITEM_FILE_QUIT:			macos9_done = true;			break;		default: break;		}		break;	case MENU_GO:		{			WindowRef front;			struct gui_window *gw;#ifdef __MACOS9__			front = FrontWindow();#else			front = NULL;#endif			gw = front != NULL ? macos9_find_window(front) : NULL;			if (gw == NULL) break;			switch (item) {			case ITEM_GO_BACK:    macos9_window_back(gw); break;			case ITEM_GO_FORWARD: macos9_window_forward(gw); break;			case ITEM_GO_RELOAD:  macos9_window_reload(gw); break;			case ITEM_GO_HOME:    macos9_window_home(gw); break;			default: break;			}		}		break;	default: break;	}}static voidmacos9_init_menus(void){	MenuHandle apple_menu, file_menu, edit_menu, go_menu;#ifdef __MACOS9__	apple_menu = NewMenu(MENU_APPLE, "\p\024");	AppendMenu(apple_menu, "\pAbout MacSurf...");	AppendMenu(apple_menu, "\p(-");	AppendResMenu(apple_menu, 'DRVR');	InsertMenu(apple_menu, 0);	file_menu = NewMenu(MENU_FILE, "\pFile");	AppendMenu(file_menu, "\pNew Window/N");	AppendMenu(file_menu, "\pOpen Location.../L");	AppendMenu(file_menu, "\pClose/W");	AppendMenu(file_menu, "\p(-");	AppendMenu(file_menu, "\pQuit/Q");	InsertMenu(file_menu, 0);	edit_menu = NewMenu(MENU_EDIT, "\pEdit");	AppendMenu(edit_menu, "\pUndo/Z");	AppendMenu(edit_menu, "\p(-");	AppendMenu(edit_menu, "\pCut/X");	AppendMenu(edit_menu, "\pCopy/C");	AppendMenu(edit_menu, "\pPaste/V");	InsertMenu(edit_menu, 0);	go_menu = NewMenu(MENU_GO, "\pGo");	AppendMenu(go_menu, "\pBack/[");	AppendMenu(go_menu, "\pForward/]");	AppendMenu(go_menu, "\pStop/.");	AppendMenu(go_menu, "\pReload/R");	AppendMenu(go_menu, "\p(-");	AppendMenu(go_menu, "\pHome/H");	InsertMenu(go_menu, 0);#else	apple_menu = NewMenu(MENU_APPLE, PSTR("\024"));	AppendMenu(apple_menu, PSTR("About MacSurf..."));	InsertMenu(apple_menu, 0);	file_menu = NewMenu(MENU_FILE, PSTR("File"));	InsertMenu(file_menu, 0);	edit_menu = NewMenu(MENU_EDIT, PSTR("Edit"));	InsertMenu(edit_menu, 0);	go_menu = NewMenu(MENU_GO, PSTR("Go"));	InsertMenu(go_menu, 0);#endif	DrawMenuBar();}/* ---- Drawing ---- */#ifdef __MACOS9__static Booleanpoint_in_rect(Point p, const Rect *r){	return (p.h >= r->left && p.h < r->right &&	        p.v >= r->top && p.v < r->bottom);}static voiddraw_url_bar(struct gui_window *gw){	Rect r = gw->url_rect;	unsigned char pstr[256];	short text_x, text_y;	short n;	EraseRect(&r);	FrameRect(&r);	TextFont(kFontIDMonaco);	TextSize(9);	TextFace(0);	n = gw->url_len;	if (n > 255) n = 255;	pstr[0] = (unsigned char)n;	memcpy(pstr + 1, gw->url, n);	text_x = (short)(r.left + MACOS9_URL_X_PADDING);	text_y = (short)(r.top + 14);	MoveTo(text_x, text_y);	DrawText(gw->url, 0, n);	if (gw->url_active) {		short cx = (short)(text_x + n * 6);		MoveTo(cx, (short)(r.top + 3));		DrawText("_", 0, 1);	}}static voiddraw_content(struct gui_window *gw){	Rect r = gw->content_rect;	long i;	short y;	EraseRect(&r);	TextFont(kFontIDMonaco);	TextSize(MACOS9_CONTENT_FONT_SIZE);	TextFace(0);	y = (short)(r.top + MACOS9_CONTENT_MARGIN + MACOS9_LINE_HEIGHT - 2);	for (i = 0; i < gw->visible_lines; i++) {		long idx = gw->top_line + i;		if (idx >= gw->line_count) break;		if (y > r.bottom - 2) break;		MoveTo((short)(r.left + MACOS9_CONTENT_MARGIN), y);		DrawText(gw->content + gw->line_offsets[idx],			0,			gw->line_lengths[idx]);		y = (short)(y + MACOS9_LINE_HEIGHT);	}	/* Status line at the bottom of the content area */	{		unsigned char s[256];		int slen = (int)strlen(gw->status);		if (slen > 255) slen = 255;		s[0] = (unsigned char)slen;		memcpy(s + 1, gw->status, slen);		TextFont(kFontIDGeneva);		TextSize(9);		MoveTo((short)(r.left + MACOS9_CONTENT_MARGIN),		       (short)(r.bottom - 4));		DrawString(s);	}}#endif /* __MACOS9__ *//* ---- Scroll action proc ---- */#ifdef __MACOS9__/* No pascal qualifier: on Carbon PPC, ControlActionUPP is a native * function pointer.  CW8 pascal calling convention does NOT match * what CarbonLib calls with, so the stack gets corrupted on the * very first scroll-arrow click. Reproduces only when content is * long enough for the scrollbar to actually engage (frogfin is * 1 line, never scrolled, never crashed). */static voidscroll_action(ControlRef ctrl, ControlPartCode part){	WindowRef win;	struct gui_window *gw;	long delta;	long new_top;	win = (WindowRef)GetControlOwner(ctrl);	gw = macos9_find_window(win);	if (gw == NULL) return;	switch (part) {	case kControlUpButtonPart:    delta = -1; break;	case kControlDownButtonPart:  delta = 1;  break;	case kControlPageUpPart:      delta = -(gw->visible_lines - 1); break;	case kControlPageDownPart:    delta = gw->visible_lines - 1; break;	default: return;	}	new_top = gw->top_line + delta;	if (new_top < 0) new_top = 0;	if (new_top > gw->line_count - gw->visible_lines)		new_top = gw->line_count - gw->visible_lines;	if (new_top < 0) new_top = 0;	if (new_top != gw->top_line) {		gw->top_line = new_top;		SetControlValue(ctrl, (short)new_top);		macos9_window_invalidate_content(gw);	}}#endif/* ---- Event handlers ---- */static voidmacos9_handle_update(const EventRecord *event){#ifdef __MACOS9__	WindowRef win = (WindowRef)(unsigned long)event->message;	struct gui_window *gw = macos9_find_window(win);	GrafPtr old_port;	Rect bounds;	if (gw == NULL) return;	GetPort(&old_port);	SetPortWindowPort(win);	BeginUpdate(win);	GetWindowPortBounds(win, &bounds);	EraseRect(&bounds);	DrawControls(win);	draw_url_bar(gw);	/* Phase 5: drive content rendering through the NetSurf core	 * pipeline (browser_window_redraw -> content_handler->redraw ->	 * html_redraw -> plotter_table -> our QuickDraw plotters).	 * Falls back to the v0.1 manual DrawText loop when there's no	 * browser_window or no content yet. */	if (gw->bw != NULL && browser_window_redraw_ready(gw->bw)) {		struct rect clip;		struct redraw_context ctx;		clip.x0 = gw->content_rect.left;		clip.y0 = gw->content_rect.top;		clip.x1 = gw->content_rect.right;		clip.y1 = gw->content_rect.bottom;		memset(&ctx, 0, sizeof(ctx));		ctx.interactive = true;		ctx.background_images = true;		ctx.plot = &macos9_plotters;		browser_window_redraw(gw->bw, gw->content_rect.left,				gw->content_rect.top, &clip, &ctx);	} else {		draw_content(gw);	}	EndUpdate(win);	SetPort(old_port);#else	(void)event;#endif}static voidmacos9_handle_activate(const EventRecord *event){#ifdef __MACOS9__	WindowRef win = (WindowRef)(unsigned long)event->message;	struct gui_window *gw = macos9_find_window(win);	if (gw == NULL) return;	macos9_window_invalidate_all(gw);#else	(void)event;#endif}static voidmacos9_handle_mouse_down(const EventRecord *event){#ifdef __MACOS9__	WindowRef win;	short part;	Point local;	struct gui_window *gw;	part = FindWindow(event->where, &win);	switch (part) {	case inMenuBar:		{			long choice = MenuSelect(event->where);			if (choice != 0) {				macos9_handle_menu((short)(choice >> 16),					(short)(choice & 0xFFFF));				HiliteMenu(0);			}		}		break;	case inDrag:		{			Rect limits = qd.screenBits.bounds;			DragWindow(win, event->where, &limits);		}		break;	case inGoAway:		if (TrackGoAway(win, event->where)) {			gw = macos9_find_window(win);			if (gw != NULL) {				macos9_window_destroy(gw);				if (macos9_find_window(FrontWindow()) == NULL &&				    FrontWindow() == NULL)					macos9_done = true;			}		}		break;	case inGrow:		{			Rect limits;			long new_size;			SetRect(&limits, 320, 200, 1600, 1200);			new_size = GrowWindow(win, event->where, &limits);			if (new_size != 0) {				short h = (short)(new_size & 0xFFFF);				short v = (short)((new_size >> 16) & 0xFFFF);				SizeWindow(win, h, v, true);				gw = macos9_find_window(win);				if (gw != NULL) {					macos9_window_layout(gw);					macos9_window_rewrap(gw);					macos9_window_update_scrollbar(gw);					macos9_window_invalidate_all(gw);				}			}		}		break;	case inContent:		if (win != FrontWindow()) {			SelectWindow(win);			break;		}		gw = macos9_find_window(win);		if (gw == NULL) break;		SetPortWindowPort(win);		local = event->where;		GlobalToLocal(&local);		/* URL rect hit? */		if (point_in_rect(local, &gw->url_rect)) {			gw->url_active = true;			macos9_window_invalidate_url(gw);			break;		}		/* Control hit? */		{			ControlRef ctrl;			short cpart;			cpart = FindControl(local, win, &ctrl);			if (ctrl != NULL) {				if (ctrl == gw->scroll_v && cpart != 0) {					/* Scroll without a ControlActionUPP.  All parts					 * (arrows, page, thumb) are handled the same way:					 * let CarbonLib track, then read the new value.					 * This avoids the UPP calling-convention crash					 * that persists even without pascal. */					TrackControl(ctrl, local, NULL);					gw->top_line = GetControlValue(ctrl);					macos9_window_invalidate_content(gw);				} else if (cpart != 0) {					if (TrackControl(ctrl, local, NULL) != 0) {						gw->url_active = false;						if (ctrl == gw->back_btn)							macos9_window_back(gw);						else if (ctrl == gw->forward_btn)							macos9_window_forward(gw);						else if (ctrl == gw->reload_btn)							macos9_window_reload(gw);						else if (ctrl == gw->home_btn)							macos9_window_home(gw);					}				}				break;			}		}		/* Click outside URL rect while url_active commits nothing. */		if (gw->url_active) {			gw->url_active = false;			macos9_window_invalidate_url(gw);		}		break;	default: break;	}#else	(void)event;#endif}static voidmacos9_handle_key_down(const EventRecord *event){#ifdef __MACOS9__	WindowRef win = FrontWindow();	struct gui_window *gw;	char ch = (char)(event->message & 0xFF);	if (event->modifiers & cmdKey) {		long choice = MenuKey((short)ch);		if (choice != 0) {			macos9_handle_menu((short)(choice >> 16),				(short)(choice & 0xFFFF));			HiliteMenu(0);		}		return;	}	if (win == NULL) return;	gw = macos9_find_window(win);	if (gw == NULL) return;	if (!gw->url_active) return;	if (ch == 0x0D || ch == 0x03) { /* Return / Enter */		gw->url[gw->url_len] = '\0';		macos9_window_navigate(gw, gw->url);		return;	}	if (ch == 0x1B) { /* Esc */		gw->url_active = false;		macos9_window_invalidate_url(gw);		return;	}	if (ch == 0x08 || ch == 0x7F) { /* Backspace / Delete */		if (gw->url_len > 0) {			gw->url_len--;			gw->url[gw->url_len] = '\0';			macos9_window_invalidate_url(gw);		}		return;	}	if (ch >= 0x20 && ch < 0x7F) {		if (gw->url_len < MACSURF_URL_MAX - 1) {			gw->url[gw->url_len++] = ch;			gw->url[gw->url_len] = '\0';			macos9_window_invalidate_url(gw);		}		return;	}#else	(void)event;#endif}static voidmacos9_handle_os_event(const EventRecord *event){	(void)event;}static voidmacos9_handle_high_level_event(const EventRecord *event){	(void)event;}static voidmacos9_dispatch_event(const EventRecord *event){	switch (event->what) {	case nullEvent:                               break;	case mouseDown:   macos9_handle_mouse_down(event); break;	case keyDown:	case autoKey:     macos9_handle_key_down(event); break;	case updateEvt:   macos9_handle_update(event);   break;	case activateEvt: macos9_handle_activate(event); break;	case osEvt:       macos9_handle_os_event(event); break;	case 23:          macos9_handle_high_level_event(event); break;	default:          break;	}}/* * Phase 5: dispatch any queued URL via the real NetSurf core pipeline. * If gw->bw is set we go through browser_window_navigate (which drives * the macos9 HTTP fetcher, llcache, content_factory, html handler and * plotters end-to-end). If bw is NULL we still fall back to the v0.1 * raw fetch path so the URL bar remains usable in degenerate cases. */static voidmacos9_run_pending_fetches(void){	WindowRef w;	int guard;	guard = 0;	w = FrontWindow();	while (w != NULL && guard < 32) {		struct gui_window *gw = macos9_find_window(w);		if (gw != NULL && gw->fetch_pending) {			gw->fetch_pending = false;			macos9_fetching = true;			if (gw->bw != NULL) {				nsurl *u = NULL;				if (nsurl_create(gw->url, &u) == NSERROR_OK) {					browser_window_navigate(gw->bw, u,						NULL, BW_NAVIGATE_HISTORY,						NULL, NULL, NULL);					nsurl_unref(u);				}			} else {				macos9_fetch_url(gw->url, gw,						macos9_fetch_callback);			}			macos9_fetching = false;		}		w = GetNextWindow(w);		guard++;	}}static voidmacos9_poll(void){	EventRecord event;	UInt32 sleep_ticks;	macos9_run_pending_fetches();	if (macos9_fetching) sleep_ticks = 1;	else sleep_ticks = (UInt32)macos9_get_next_delay();	WaitNextEvent(everyEvent, &event, sleep_ticks, NULL);	macos9_dispatch_event(&event);#ifdef WITH_DUKTAPE	/* Drive every live JS heap's timer queue once per event-loop	 * pass so setTimeout/setInterval callbacks fire even when no	 * script is currently running. */	macsurf_js_pump_all();#endif	macos9_schedule_run();}intmain(int argc, char **argv){	nserror ret;	struct netsurf_table macos9_table;	struct gui_window *initial_win;	(void)argc;	(void)argv;	memset(&macos9_table, 0, sizeof(macos9_table));	macos9_table.misc = &macos9_misc_table;	macos9_table.window = macos9_window_table;	macos9_table.download = macos9_download_table;	macos9_table.clipboard = macos9_clipboard_table;	macos9_table.fetch = &macos9_fetch_table;	macos9_table.utf8 = macos9_utf8_table;	macos9_table.bitmap = macos9_bitmap_table;	macos9_table.layout = macos9_layout_table;#ifndef TARGET_API_MAC_CARBON	MaxApplZone();	MoreMasters();	MoreMasters();	InitGraf(&qd.thePort);	InitFonts();	InitWindows();	InitMenus();	TEInit();	InitDialogs(NULL);#endif	InitCursor();#if TARGET_API_MAC_CARBON	{		long appearance_result;		if (Gestalt(gestaltAppearanceAttr, &appearance_result) == noErr)			RegisterAppearanceClient();	}#endif	FlushEvents(everyEvent, 0);#ifdef __MACOS9__	{		long gestalt_val;		if (Gestalt(gestaltOpenTpt, &gestalt_val) == noErr &&		    (gestalt_val & gestaltOpenTptPresentMask) &&		    (gestalt_val & gestaltOpenTptTCPPresentMask)) {			if (InitOpenTransportInContext(kInitOTForApplicationMask, &macos9_ot_context) == noErr) {				ot_initialized = true;			}		}	}#endif	macos9_init_menus();	ret = netsurf_register(&macos9_table);	(void)ret;	/* nsoption_init must run before netsurf_init. Otherwise the	 * nsoptions[] table has NULL key strings and	 * ns_system_colour_char (called transitively from netsurf_init	 * -> nscolour_update) crashes inside strcmp. */	ret = nsoption_init(NULL, NULL, NULL);	(void)ret;	/* Register HTTP/HTTPS fetcher before netsurf_init so the	 * fetch system knows how to handle http: URLs. */	{		extern nserror macos9_fetcher_register(void);		ret = macos9_fetcher_register();		(void)ret;	}	ret = netsurf_init(NULL);	(void)ret;	/*	 * Phase 5: route initial window creation through NetSurf core so	 * a real browser_window is created and our gui_window adopts its	 * bw via the gui_window_table.create callback. browser_window_create	 * with a non-NULL url also triggers the first navigation, so the	 * home page begins fetching immediately through the real pipeline.	 *	 * If OT failed to initialise, fall back to the v0.1 standalone	 * gui_window so the URL bar still works.	 */	/* v0.3: try the NetSurf core pipeline first.  Falls back to	 * v0.1 strip+text if the core pipeline fails. */	initial_win = NULL;	if (ot_initialized) {		nsurl *home_url = NULL;		struct browser_window *bw = NULL;		if (nsurl_create(MACSURF_HOME_URL, &home_url) == NSERROR_OK) {			browser_window_create(				BW_CREATE_HISTORY | BW_CREATE_FOREGROUND,				home_url, NULL, NULL, &bw);			nsurl_unref(home_url);		}#ifdef __MACOS9__		if (FrontWindow() != NULL)			initial_win = macos9_find_window(FrontWindow());#endif	}	if (initial_win == NULL)		initial_win = macos9_create_initial_window();#ifdef WITH_DUKTAPE	/* JS smoke test - visible proof of life in the status line	 * before any page loads. Result lands in initial_win->status,	 * which the content area paints at its bottom edge. */	if (initial_win != NULL) {		jsheap *smoke_heap = NULL;		if (js_newheap(0, &smoke_heap) == NSERROR_OK &&		    smoke_heap != NULL) {			unsigned char ok;			ok = (unsigned char)js_exec((jsthread *)smoke_heap,				(const unsigned char *)"1+1==2",				6, "smoke");			/* Title is set once and survives the page load that			 * follows; status would be overwritten by the home			 * navigation. */			{				unsigned char ptitle[64];				const char *t = ok ?					"MacSurf - JS OK (1+1=2)" :					"MacSurf - JS FAIL";				unsigned long tn = (unsigned long)strlen(t);				if (tn > 63) tn = 63;				ptitle[0] = (unsigned char)tn;				memcpy(ptitle + 1, t, tn);#ifdef __MACOS9__				if (initial_win->window != NULL)					SetWTitle(initial_win->window, ptitle);#endif			}			js_destroyheap(smoke_heap);		} else {#ifdef __MACOS9__			if (initial_win->window != NULL)				SetWTitle(initial_win->window,					(const unsigned char *)"\025MacSurf - JS heap FAIL");#endif		}		macos9_window_invalidate_all(initial_win);	}#endif	if (initial_win != NULL) {		if (ot_initialized) {			macos9_window_home(initial_win);		} else {			strncpy(initial_win->status,				"No TCP/IP - fetches disabled",				sizeof(initial_win->status) - 1);			initial_win->status[sizeof(initial_win->status) - 1] =					'\0';			macos9_window_invalidate_content(initial_win);		}	}	while (!macos9_done) {		macos9_poll();	}#ifdef __MACOS9__	if (ot_initialized) {		CloseOpenTransportInContext(macos9_ot_context); macos9_ot_context = NULL;		ot_initialized = false;	}#endif	netsurf_exit();	return 0;}