/* * MacSurf - window.c * Carbon window with a toolbar (Back / Forward / Reload / Home push buttons * and a manually drawn URL field) plus a vertical scroll bar and a custom * text content area. All Appearance Manager controls created via NewControl. * * URL editing is handled manually (no TextEdit, no kControlEditTextProc) - * keystrokes append to gui_window::url while gui_window::url_active is true, * Return commits and fetches, Escape cancels. */#include <stdlib.h>#include <string.h>#include "utils/errors.h"#include "utils/log.h"#include "netsurf/types.h"#include "netsurf/window.h"#include "netsurf/browser_window.h"#include "macos9/macos9.h"#ifdef __MACOS9__#include <MacWindows.h>#include <Controls.h>#include <Appearance.h>#include <Quickdraw.h>#include <TextUtils.h>#include <Fonts.h>/* CW8's Controls.h lacks these Appearance Manager CDEF procIDs. */#ifndef kControlPushButtonProc#define kControlPushButtonProc    368#endif#ifndef kControlScrollBarLiveProc#define kControlScrollBarLiveProc 386#endif#else#ifndef __MACTYPES__typedef int OSStatus;typedef int Boolean;#endiftypedef short SInt16;typedef void *CFStringRef;typedef unsigned char Str255[256];typedef unsigned long WindowClass;typedef unsigned long WindowAttributes;enum { kDocumentWindowClass = 6 };enum { kWindowStandardDocumentAttributes = 0x1F };enum { kWindowContentRgn = 33 };enum { kControlPushButtonProc = 368, kControlScrollBarLiveProc = 386 };#define noErr 0#define PSTR(s) ((const unsigned char *)(s))static void SetRect(Rect *r, short l, short t, short rt, short b){ r->top=t; r->left=l; r->bottom=b; r->right=rt; }static OSStatus CreateNewWindow(WindowClass wc, WindowAttributes attr,		const Rect *b, WindowRef *out){ (void)wc;(void)attr;(void)b; static int d; *out=&d; return 0; }static ControlRef NewControl(WindowRef w, const Rect *b, const unsigned char *t,		Boolean vis, short v, short min, short max, short proc, long ref){ (void)w;(void)b;(void)t;(void)vis;(void)v;(void)min;(void)max;(void)proc;(void)ref; return NULL; }static void ShowWindow(WindowRef w) { (void)w; }static void DisposeWindow(WindowRef w) { (void)w; }static void SetWTitle(WindowRef w, const unsigned char *t) { (void)w;(void)t; }static void GetWindowBounds(WindowRef w, int rgn, Rect *b){ (void)w;(void)rgn; b->top=0;b->left=0;b->bottom=480;b->right=640; }static void InvalWindowRect(WindowRef w, const Rect *r) { (void)w;(void)r; }static void SetControlValue(ControlRef c, short v) { (void)c;(void)v; }static void SetControlMaximum(ControlRef c, short v) { (void)c;(void)v; }static void SetControlMinimum(ControlRef c, short v) { (void)c;(void)v; }static void MoveControl(ControlRef c, short h, short v) { (void)c;(void)h;(void)v; }static void SizeControl(ControlRef c, short h, short v) { (void)c;(void)h;(void)v; }static void SetWRefCon(WindowRef w, long r) { (void)w;(void)r; }#endifstatic struct gui_window *window_list = NULL;/* ---- Public helpers ---- */struct gui_window *macos9_find_window(WindowRef w){	struct gui_window *gw;	for (gw = window_list; gw != NULL; gw = gw->next) {		if (gw->window == w)			return gw;	}	return NULL;}static voidset_pstring(unsigned char *dst, const char *src){	size_t len;	len = strlen(src);	if (len > 255) len = 255;	dst[0] = (unsigned char)len;	memcpy(dst + 1, src, len);}/* * Compute toolbar, URL and content rects from the window content bounds. * Called on create and whenever the window is resized. */voidmacos9_window_layout(struct gui_window *gw){	Rect content;	short win_w, win_h;	short url_x, url_right;	if (gw->window == NULL) return;	GetWindowBounds(gw->window, kWindowContentRgn, &content);	win_w = (short)(content.right - content.left);	win_h = (short)(content.bottom - content.top);	SetRect(&gw->toolbar_rect, 0, 0, win_w, MACOS9_TOOLBAR_HEIGHT);	url_x = MACOS9_BTN_GAP	      + (MACOS9_BTN_WIDTH + MACOS9_BTN_GAP) * 4;	url_right = win_w - MACOS9_BTN_GAP;	SetRect(&gw->url_rect,		url_x,		MACOS9_BTN_Y,		url_right,		MACOS9_BTN_Y + MACOS9_BTN_HEIGHT);	SetRect(&gw->content_rect,		0,		MACOS9_TOOLBAR_HEIGHT,		win_w - MACOS9_SCROLLBAR_WIDTH,		win_h);	if (gw->scroll_v != NULL) {		MoveControl(gw->scroll_v,			(short)(win_w - MACOS9_SCROLLBAR_WIDTH),			(short)(MACOS9_TOOLBAR_HEIGHT - 1));		SizeControl(gw->scroll_v,			MACOS9_SCROLLBAR_WIDTH + 1,			(short)(win_h - MACOS9_TOOLBAR_HEIGHT + 2));	}	gw->visible_lines = (long)((win_h - MACOS9_TOOLBAR_HEIGHT -			MACOS9_CONTENT_MARGIN * 2) / MACOS9_LINE_HEIGHT);	if (gw->visible_lines < 1) gw->visible_lines = 1;}voidmacos9_window_invalidate_all(struct gui_window *gw){	Rect r;	if (gw->window == NULL) return;	GetWindowBounds(gw->window, kWindowContentRgn, &r);	r.right -= r.left;	r.bottom -= r.top;	r.left = 0;	r.top = 0;	InvalWindowRect(gw->window, &r);}voidmacos9_window_invalidate_url(struct gui_window *gw){	if (gw->window == NULL) return;	InvalWindowRect(gw->window, &gw->url_rect);}voidmacos9_window_invalidate_content(struct gui_window *gw){	if (gw->window == NULL) return;	InvalWindowRect(gw->window, &gw->content_rect);}/* * Re-wrap content[] into the line_offsets/line_lengths arrays using the * current content_rect width. Called after a fetch completes and whenever * the window size changes. */voidmacos9_window_rewrap(struct gui_window *gw){	short usable_px;	short chars_per_line;	usable_px = (short)((gw->content_rect.right - gw->content_rect.left) -			MACOS9_CONTENT_MARGIN * 2);	if (usable_px < 40) usable_px = 40;	/* Monaco 10 is approximately 6 px per char. Conservative. */	chars_per_line = (short)(usable_px / 6);	if (chars_per_line < 8) chars_per_line = 8;	if (chars_per_line > 500) chars_per_line = 500;	gw->line_count = macos9_word_wrap(gw->content, gw->content_len,			gw->line_offsets, gw->line_lengths,			MACSURF_MAX_LINES, chars_per_line);	if (gw->top_line >= gw->line_count)		gw->top_line = gw->line_count > 0 ? gw->line_count - 1 : 0;}voidmacos9_window_update_scrollbar(struct gui_window *gw){	long max;	if (gw->scroll_v == NULL) return;	max = gw->line_count - gw->visible_lines;	if (max < 0) max = 0;	if (max > 32000) max = 32000;	SetControlMinimum(gw->scroll_v, 0);	SetControlMaximum(gw->scroll_v, (short)max);	if (gw->top_line > max) gw->top_line = max;	if (gw->top_line < 0) gw->top_line = 0;	SetControlValue(gw->scroll_v, (short)gw->top_line);}/* ---- History + navigation ---- */static voidhistory_push(struct gui_window *gw, const char *url){	int i;	/* If we navigated back and now go somewhere new, truncate forward. */	if (gw->history_index + 1 < gw->history_count) {		gw->history_count = gw->history_index + 1;	}	if (gw->history_count >= MACSURF_HISTORY_MAX) {		for (i = 1; i < MACSURF_HISTORY_MAX; i++) {			memcpy(gw->history[i - 1], gw->history[i], MACSURF_URL_MAX);		}		gw->history_count = MACSURF_HISTORY_MAX - 1;		gw->history_index = gw->history_count - 1;	}	strncpy(gw->history[gw->history_count], url, MACSURF_URL_MAX - 1);	gw->history[gw->history_count][MACSURF_URL_MAX - 1] = '\0';	gw->history_index = gw->history_count;	gw->history_count++;}static voidset_status(struct gui_window *gw, const char *msg){	if (msg == NULL) { gw->status[0] = '\0'; return; }	strncpy(gw->status, msg, sizeof(gw->status) - 1);	gw->status[sizeof(gw->status) - 1] = '\0';}voidmacos9_window_navigate(struct gui_window *gw, const char *url){	if (url == NULL || *url == '\0') return;	strncpy(gw->url, url, MACSURF_URL_MAX - 1);	gw->url[MACSURF_URL_MAX - 1] = '\0';	gw->url_len = (short)strlen(gw->url);	gw->url_active = false;	history_push(gw, gw->url);	set_status(gw, "Loading...");	gw->fetch_pending = true;	macos9_window_invalidate_url(gw);	macos9_window_invalidate_content(gw);}voidmacos9_window_back(struct gui_window *gw){	if (gw->history_index <= 0) return;	gw->history_index--;	strncpy(gw->url, gw->history[gw->history_index], MACSURF_URL_MAX - 1);	gw->url[MACSURF_URL_MAX - 1] = '\0';	gw->url_len = (short)strlen(gw->url);	gw->url_active = false;	set_status(gw, "Loading...");	gw->fetch_pending = true;	macos9_window_invalidate_all(gw);}voidmacos9_window_forward(struct gui_window *gw){	if (gw->history_index + 1 >= gw->history_count) return;	gw->history_index++;	strncpy(gw->url, gw->history[gw->history_index], MACSURF_URL_MAX - 1);	gw->url[MACSURF_URL_MAX - 1] = '\0';	gw->url_len = (short)strlen(gw->url);	gw->url_active = false;	set_status(gw, "Loading...");	gw->fetch_pending = true;	macos9_window_invalidate_all(gw);}voidmacos9_window_reload(struct gui_window *gw){	if (gw->url[0] == '\0') return;	set_status(gw, "Loading...");	gw->fetch_pending = true;	macos9_window_invalidate_content(gw);}voidmacos9_window_home(struct gui_window *gw){	macos9_window_navigate(gw, MACSURF_HOME_URL);}/* ---- Window creation ---- */static struct gui_window *macos9_window_create(struct browser_window *bw,		struct gui_window *existing,		gui_window_create_flags flags){	struct gui_window *gw;	Rect bounds;	Rect btn_rect;	Rect sb_rect;	OSStatus err;	short x;	(void)existing;	(void)flags;	gw = calloc(1, sizeof(*gw));	if (gw == NULL) return NULL;	gw->bw = bw;#ifdef __MACOS9__	SetRect(&bounds, 40, 50, 640, 470);#else	bounds.left = 40; bounds.top = 50;	bounds.right = 640; bounds.bottom = 470;#endif	err = CreateNewWindow(kDocumentWindowClass,			kWindowStandardDocumentAttributes,			&bounds, &gw->window);	if (err != noErr) {		free(gw);		return NULL;	}#ifdef __MACOS9__	SetWTitle(gw->window, "\pMacSurf");	SetWRefCon(gw->window, (long)gw);#else	SetWTitle(gw->window, PSTR("MacSurf"));#endif	x = MACOS9_BTN_GAP;	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->back_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pBack",#else			PSTR("Back"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	x = (short)(x + MACOS9_BTN_WIDTH + MACOS9_BTN_GAP);	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->forward_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pFwd",#else			PSTR("Fwd"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	x = (short)(x + MACOS9_BTN_WIDTH + MACOS9_BTN_GAP);	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->reload_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pReload",#else			PSTR("Reload"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	x = (short)(x + MACOS9_BTN_WIDTH + MACOS9_BTN_GAP);	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->home_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pHome",#else			PSTR("Home"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	SetRect(&sb_rect,		(short)(bounds.right - bounds.left - MACOS9_SCROLLBAR_WIDTH),		(short)(MACOS9_TOOLBAR_HEIGHT - 1),		(short)(bounds.right - bounds.left + 1),		(short)(bounds.bottom - bounds.top + 1));	gw->scroll_v = NewControl(gw->window, &sb_rect,#ifdef __MACOS9__			"\p",#else			PSTR(""),#endif			true, 0, 0, 0, kControlScrollBarLiveProc, (long)gw);	strncpy(gw->url, MACSURF_HOME_URL, MACSURF_URL_MAX - 1);	gw->url[MACSURF_URL_MAX - 1] = '\0';	gw->url_len = (short)strlen(gw->url);	gw->url_active = false;	gw->url_scroll = 0;	gw->content_len = 0;	gw->content[0] = '\0';	gw->line_count = 0;	gw->top_line = 0;	gw->visible_lines = 20;	gw->history_count = 0;	gw->history_index = -1;	gw->fetch_pending = false;	set_status(gw, "Ready");	macos9_window_layout(gw);	ShowWindow(gw->window);	gw->next = window_list;	window_list = gw;	return gw;}voidmacos9_window_destroy(struct gui_window *gw){	struct gui_window **p;	for (p = &window_list; *p != NULL; p = &(*p)->next) {		if (*p == gw) {			*p = gw->next;			break;		}	}	if (gw->window != NULL)		DisposeWindow(gw->window);	free(gw);}/* ---- gui_window_table stubs (NetSurf core is stubbed out) ---- */static nserrormacos9_window_invalidate(struct gui_window *gw, const struct rect *rect){	(void)rect;	macos9_window_invalidate_content(gw);	return NSERROR_OK;}static boolmacos9_window_get_scroll(struct gui_window *gw, int *sx, int *sy){	*sx = 0;	*sy = (int)(gw->top_line * MACOS9_LINE_HEIGHT);	return true;}static nserrormacos9_window_set_scroll(struct gui_window *gw, const struct rect *rect){	if (rect != NULL) {		gw->top_line = rect->y0 / MACOS9_LINE_HEIGHT;		if (gw->top_line < 0) gw->top_line = 0;		macos9_window_update_scrollbar(gw);		macos9_window_invalidate_content(gw);	}	return NSERROR_OK;}static nserrormacos9_window_get_dimensions(struct gui_window *gw, int *width, int *height){	*width = gw->content_rect.right - gw->content_rect.left;	*height = gw->content_rect.bottom - gw->content_rect.top;	return NSERROR_OK;}static nserrormacos9_window_event(struct gui_window *gw, enum gui_window_event event){	(void)gw;	(void)event;	return NSERROR_OK;}static voidmacos9_window_set_title(struct gui_window *gw, const char *title){	Str255 ptitle;	if (gw->window == NULL || title == NULL) return;	set_pstring(ptitle, title);	SetWTitle(gw->window, ptitle);}static nserrormacos9_window_set_url(struct gui_window *gw, struct nsurl *url){	(void)gw;	(void)url;	return NSERROR_OK;}static void macos9_window_set_icon(struct gui_window *g, struct hlcache_handle *i){ (void)g; (void)i; }static void macos9_window_set_status(struct gui_window *g, const char *text){	if (g != NULL && text != NULL) {		set_status(g, text);	}}static void macos9_window_set_pointer(struct gui_window *g, enum gui_pointer_shape s){ (void)g; (void)s; }static void macos9_window_place_caret(struct gui_window *g, int x, int y, int h,		const struct rect *clip){ (void)g;(void)x;(void)y;(void)h;(void)clip; }static bool macos9_window_drag_start(struct gui_window *g, gui_drag_type t,		const struct rect *r){ (void)g;(void)t;(void)r; return false; }static nserror macos9_window_save_link(struct gui_window *g, struct nsurl *u,		const char *t){ (void)g;(void)u;(void)t; return NSERROR_OK; }static void macos9_window_create_form_select_menu(struct gui_window *g,		struct form_control *c){ (void)g;(void)c; }static void macos9_window_file_gadget_open(struct gui_window *g,		struct hlcache_handle *h, struct form_control *f){ (void)g;(void)h;(void)f; }static void macos9_window_drag_save_object(struct gui_window *g,		struct hlcache_handle *c, gui_save_type t){ (void)g;(void)c;(void)t; }static void macos9_window_drag_save_selection(struct gui_window *g, const char *s){ (void)g;(void)s; }static void macos9_window_console_log(struct gui_window *g,		browser_window_console_source src, const char *m, size_t l,		browser_window_console_flags f){ (void)g;(void)src;(void)m;(void)l;(void)f; }struct gui_window *macos9_create_initial_window(void){	return macos9_window_create(NULL, NULL, 0);}static struct gui_window_table window_table = {	macos9_window_create,	macos9_window_destroy,	macos9_window_invalidate,	(unsigned char (*)(struct gui_window *, int *, int *))		macos9_window_get_scroll,	macos9_window_set_scroll,	macos9_window_get_dimensions,	macos9_window_event,	macos9_window_set_title,	macos9_window_set_url,	macos9_window_set_icon,	macos9_window_set_status,	macos9_window_set_pointer,	macos9_window_place_caret,	(unsigned char (*)(struct gui_window *, gui_drag_type, const struct rect *))		macos9_window_drag_start,	macos9_window_save_link,	macos9_window_create_form_select_menu,	macos9_window_file_gadget_open,	macos9_window_drag_save_object,	macos9_window_drag_save_selection,	macos9_window_console_log};struct gui_window_table *macos9_window_table = &window_table;