/* * MacSurf - window.c * * Carbon window with toolbar (Back / Forward / Reload / Home), a * TextEdit URL field, vertical and horizontal scroll bars, content * area, and status strip. Rendering flows entirely through the * NetSurf core via browser_window_redraw and the macos9 plotter * table. * * Key design points: * *   TextEdit URL field (url_te). Created via TENew after *   SetPortWindowPort + SetWRefCon — see CLAUDE.md "Known Gotchas" *   on the dsMemWZErr crash mode. TEClick / TEKey / TEActivate / *   TEDeactivate / TEIdle wired per the Inside Mac: Text flow. * *   Pixel-scroll model. scroll_x / scroll_y are pixel offsets into *   the rendered content; update_extents re-queries *   browser_window_get_extents whenever GW_EVENT_UPDATE_EXTENT or *   GW_EVENT_NEW_CONTENT fires. * *   Deferred-reformat on resize. macos9_window_resize sets a *   needs_reformat flag; macos9_windows_process_deferred handles it *   on the next poll pass, guarded by reformat_in_progress to *   prevent re-entrancy. * *   Scroll bar click. TrackControl is called with NULL action -- *   kControlScrollBarLiveProc (386) live-tracks the thumb inside *   the CDEF, so GetControlValue on return holds the final drag *   position. Arrow / page parts scroll by a single step each *   click, driven by the ControlPartCode the caller captured *   from FindControl. No ControlActionUPP is involved -- see *   Known Gotchas in CLAUDE.md for why. */#include <stdlib.h>#include <string.h>#include "utils/errors.h"#include "utils/log.h"#include "utils/nsurl.h"#include "netsurf/types.h"#include "netsurf/window.h"#include "netsurf/browser_window.h"#include "desktop/browser_history.h"#include "macos9/macos9.h"#include "macsurf_debug.h"#include "macsurf_debug_log.h"#ifdef __MACOS9__#include <MacWindows.h>#include <Controls.h>#include <Appearance.h>#include <Quickdraw.h>#include <TextUtils.h>#include <TextEdit.h>#include <Fonts.h>#include <MacMemory.h>/* CW8's Controls.h lacks these Appearance Manager CDEF procIDs. */#ifndef kControlPushButtonProc#define kControlPushButtonProc    368#endif#ifndef kControlScrollBarLiveProc#define kControlScrollBarLiveProc 386#endif/* CW8's Controls.h lacks these part codes. */#ifndef kControlUpButtonPart#define kControlUpButtonPart    20#endif#ifndef kControlDownButtonPart#define kControlDownButtonPart  21#endif#ifndef kControlPageUpPart#define kControlPageUpPart      22#endif#ifndef kControlPageDownPart#define kControlPageDownPart    23#endif#ifndef kControlIndicatorPart#define kControlIndicatorPart   129#endif/* fixes147 -- the UPP macro override that used to live here was the * root cause of the scroll-bar click crash. It cast scroll_action's * raw PPC function pointer to ControlActionUPP and passed it to * TrackControl; CarbonLib's MixedMode dispatch reads descriptor * fields from that pointer, resolves a garbage routine address * (commonly 0), and jumps into low memory. Crash signature: * PC=0x00000008, LR=0x00000004 (bl to address 0 from address 0). * Fix: drop the action proc entirely; scroll-bar handling is now * driven by the ControlPartCode returned from FindControl plus the * CDEF's live thumb value on TrackControl return. */#else/* Linux cross-check stubs. */#ifndef __MACTYPES__typedef int OSStatus;typedef int Boolean;#endiftypedef short SInt16;typedef void *CFStringRef;typedef unsigned char Str255[256];typedef unsigned long WindowClass;typedef unsigned long WindowAttributes;typedef short ControlPartCode;typedef void (*ControlActionProcPtr)(ControlRef, ControlPartCode);typedef ControlActionProcPtr ControlActionUPP;typedef void *GrafPtr;typedef void *Handle;typedef Handle CharsHandle;enum { kDocumentWindowClass = 6 };enum { kWindowStandardDocumentAttributes = 0x1F };enum { kWindowContentRgn = 33 };enum { kControlPushButtonProc = 368, kControlScrollBarLiveProc = 386 };enum { kControlUpButtonPart = 20, kControlDownButtonPart = 21,	kControlPageUpPart = 22, kControlPageDownPart = 23,	kControlIndicatorPart = 129 };enum { shiftKey = 0x0200 };#define noErr 0#define PSTR(s) ((const unsigned char *)(s))typedef struct { short v, h; } Point;struct TERec { Rect destRect, viewRect; };typedef struct TERec **TEHandleImpl;static void SetRect(Rect *r, short l, short t, short rt, short b){ r->top=t; r->left=l; r->bottom=b; r->right=rt; }static OSStatus CreateNewWindow(WindowClass wc, WindowAttributes attr,		const Rect *b, WindowRef *out){ (void)wc;(void)attr;(void)b; static int d; *out=&d; return 0; }static ControlRef NewControl(WindowRef w, const Rect *b, const unsigned char *t,		Boolean vis, short v, short min, short max, short proc, long ref){ (void)w;(void)b;(void)t;(void)vis;(void)v;(void)min;(void)max;(void)proc;(void)ref; return NULL; }static void ShowWindow(WindowRef w) { (void)w; }static void DisposeWindow(WindowRef w) { (void)w; }static void SetWTitle(WindowRef w, const unsigned char *t) { (void)w;(void)t; }static void GetWindowBounds(WindowRef w, int rgn, Rect *b){ (void)w;(void)rgn; b->top=0;b->left=0;b->bottom=480;b->right=640; }static void InvalWindowRect(WindowRef w, const Rect *r) { (void)w;(void)r; }static short GetControlValue(ControlRef c) { (void)c; return 0; }static short GetControlMaximum(ControlRef c) { (void)c; return 0; }static void SetControlValue(ControlRef c, short v) { (void)c;(void)v; }static void SetControlMaximum(ControlRef c, short v) { (void)c;(void)v; }static void SetControlMinimum(ControlRef c, short v) { (void)c;(void)v; }static void HiliteControl(ControlRef c, short s) { (void)c;(void)s; }static void MoveControl(ControlRef c, short h, short v) { (void)c;(void)h;(void)v; }static void SizeControl(ControlRef c, short h, short v) { (void)c;(void)h;(void)v; }static void SetWRefCon(WindowRef w, long r) { (void)w;(void)r; }static short TrackControl(ControlRef c, Point p, ControlActionUPP a){ (void)c;(void)p;(void)a; return 0; }static WindowRef GetControlOwner(ControlRef c) { (void)c; return NULL; }static TEHandle TENew(const Rect *dest, const Rect *view){ (void)dest; (void)view; return NULL; }static void TEDispose(TEHandle te) { (void)te; }static void TEActivate(TEHandle te) { (void)te; }static void TEDeactivate(TEHandle te) { (void)te; }static void TEIdle(TEHandle te) { (void)te; }static void TEKey(char c, TEHandle te) { (void)c; (void)te; }static void TEClick(Point p, Boolean ext, TEHandle te) { (void)p;(void)ext;(void)te; }static void TEUpdate(const Rect *r, TEHandle te) { (void)r;(void)te; }static void TESetText(const void *t, long len, TEHandle te) { (void)t;(void)len;(void)te; }static CharsHandle TEGetText(TEHandle te) { (void)te; return NULL; }static void TECalText(TEHandle te) { (void)te; }static void TextFont(short f) { (void)f; }static void TextSize(short s) { (void)s; }static void TextFace(short f) { (void)f; }static void SetPort(GrafPtr p) { (void)p; }static void GetPort(GrafPtr *p) { (void)p; }static void SetPortWindowPort(WindowRef w) { (void)w; }static void EraseRect(const Rect *r) { (void)r; }static void FrameRect(const Rect *r) { (void)r; }static void HLock(Handle h) { (void)h; }static void HUnlock(Handle h) { (void)h; }static long GetHandleSize(Handle h) { (void)h; return 0; }#define kFontIDMonaco 4#define kFontIDGeneva 3#endifstatic struct gui_window *window_list = NULL;/* ---- Internal helpers ---- */struct gui_window *macos9_find_window(WindowRef w){	struct gui_window *gw;	for (gw = window_list; gw != NULL; gw = gw->next) {		if (gw->window == w)			return gw;	}	return NULL;}static voidset_pstring(unsigned char *dst, const char *src){	size_t len;	len = strlen(src);	if (len > 255) len = 255;	dst[0] = (unsigned char)len;	memcpy(dst + 1, src, len);}static voidset_status_text(struct gui_window *gw, const char *msg){	if (msg == NULL) { gw->status[0] = '\0'; return; }	strncpy(gw->status, msg, sizeof(gw->status) - 1);	gw->status[sizeof(gw->status) - 1] = '\0';}/* ---- Layout ---- */static voidcompute_url_te_rect(const Rect *url_rect, Rect *out){	out->left   = (short)(url_rect->left   + MACOS9_URL_INSET);	out->top    = (short)(url_rect->top    + MACOS9_URL_INSET);	out->right  = (short)(url_rect->right  - MACOS9_URL_INSET);	out->bottom = (short)(url_rect->bottom - MACOS9_URL_INSET);}voidmacos9_window_layout(struct gui_window *gw){	Rect content;	short win_w, win_h;	short url_x, url_right;	short content_bottom;	short hscroll_top;	if (gw->window == NULL) return;	GetWindowBounds(gw->window, kWindowContentRgn, &content);	win_w = (short)(content.right - content.left);	win_h = (short)(content.bottom - content.top);	SetRect(&gw->toolbar_rect, 0, 0, win_w, MACOS9_TOOLBAR_HEIGHT);	url_x = MACOS9_BTN_GAP	      + (MACOS9_BTN_WIDTH + MACOS9_BTN_GAP) * 4;	url_right = (short)(win_w - MACOS9_BTN_GAP);	SetRect(&gw->url_rect,		url_x,		MACOS9_BTN_Y,		url_right,		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	hscroll_top    = (short)(win_h - MACOS9_SCROLLBAR_WIDTH);	content_bottom = (short)(hscroll_top - MACOS9_STATUS_HEIGHT);	SetRect(&gw->content_rect,		0,		MACOS9_TOOLBAR_HEIGHT,		(short)(win_w - MACOS9_SCROLLBAR_WIDTH),		content_bottom);	SetRect(&gw->status_rect,		0,		content_bottom,		(short)(win_w - MACOS9_SCROLLBAR_WIDTH),		hscroll_top);	if (gw->vscroll != NULL) {		MoveControl(gw->vscroll,			(short)(win_w - MACOS9_SCROLLBAR_WIDTH),			(short)(MACOS9_TOOLBAR_HEIGHT - 1));		SizeControl(gw->vscroll,			MACOS9_SCROLLBAR_WIDTH + 1,			(short)(content_bottom - MACOS9_TOOLBAR_HEIGHT + 2));	}	if (gw->hscroll != NULL) {		MoveControl(gw->hscroll,			(short)-1,			hscroll_top);		SizeControl(gw->hscroll,			(short)(win_w - MACOS9_SCROLLBAR_WIDTH + 2),			MACOS9_SCROLLBAR_WIDTH + 1);	}}/* ---- Invalidation ---- */voidmacos9_window_invalidate_all(struct gui_window *gw){	Rect r;	if (gw->window == NULL) return;	GetWindowBounds(gw->window, kWindowContentRgn, &r);	r.right -= r.left;	r.bottom -= r.top;	r.left = 0;	r.top = 0;	InvalWindowRect(gw->window, &r);}voidmacos9_window_invalidate_url(struct gui_window *gw){	if (gw->window == NULL) return;	InvalWindowRect(gw->window, &gw->url_rect);}voidmacos9_window_invalidate_content(struct gui_window *gw){	if (gw->window == NULL) return;	InvalWindowRect(gw->window, &gw->content_rect);}voidmacos9_window_invalidate_status(struct gui_window *gw){	if (gw->window == NULL) return;	InvalWindowRect(gw->window, &gw->status_rect);}/* ---- Extents and scroll bars ---- */static intcontent_view_width(struct gui_window *gw){	int w = gw->content_rect.right - gw->content_rect.left;	if (w < 1) w = 1;	return w;}static intcontent_view_height(struct gui_window *gw){	int h = gw->content_rect.bottom - gw->content_rect.top;	if (h < 1) h = 1;	return h;}voidmacos9_window_update_extents(struct gui_window *gw){	int w = 0;	int h = 0;	if (gw->bw != NULL) {		if (browser_window_get_extents(gw->bw, false, &w, &h) == NSERROR_OK) {			gw->content_width = w;			gw->content_height = h;		}	}	macos9_window_update_scrollbars(gw);}voidmacos9_window_update_scrollbars(struct gui_window *gw){	int view_w, view_h;	int max_x, max_y;	short cmax_x, cmax_y;	view_w = content_view_width(gw);	view_h = content_view_height(gw);	max_x = gw->content_width  - view_w;	max_y = gw->content_height - view_h;	if (max_x < 0) max_x = 0;	if (max_y < 0) max_y = 0;	if (gw->scroll_x > max_x) gw->scroll_x = max_x;	if (gw->scroll_y > max_y) gw->scroll_y = max_y;	if (gw->scroll_x < 0) gw->scroll_x = 0;	if (gw->scroll_y < 0) gw->scroll_y = 0;	cmax_x = (short)(max_x > 32000 ? 32000 : max_x);	cmax_y = (short)(max_y > 32000 ? 32000 : max_y);#ifdef __MACOS9__	/* fixes142 -- force the owning window's port before touching	 * the scrollbar controls. SetControlMaximum / SetControlValue	 * / HiliteControl internally redraw; the redraw goes to the	 * current port. This function is invoked from NetSurf's	 * GW_EVENT_NEW_CONTENT callback at arbitrary times, so the	 * caller's port is not guaranteed to be the window's. */	if (gw->window != NULL) {		SetPortWindowPort(gw->window);	}#endif	if (gw->vscroll != NULL) {		SetControlMinimum(gw->vscroll, 0);		SetControlMaximum(gw->vscroll, cmax_y);		SetControlValue(gw->vscroll, (short)gw->scroll_y);		HiliteControl(gw->vscroll, (short)(max_y > 0 ? 0 : 255));#ifdef __MACOS9__		/* fixes142 -- force immediate redraw in the now-correct		 * port, not waiting on the next update event. */		Draw1Control(gw->vscroll);#endif	}	if (gw->hscroll != NULL) {		SetControlMinimum(gw->hscroll, 0);		SetControlMaximum(gw->hscroll, cmax_x);		SetControlValue(gw->hscroll, (short)gw->scroll_x);		HiliteControl(gw->hscroll, (short)(max_x > 0 ? 0 : 255));#ifdef __MACOS9__		Draw1Control(gw->hscroll);#endif	}}voidmacos9_window_scroll_to(struct gui_window *gw, int new_x, int new_y){	int max_x, max_y;	int view_w, view_h;	view_w = content_view_width(gw);	view_h = content_view_height(gw);	max_x = gw->content_width  - view_w;	max_y = gw->content_height - view_h;	if (max_x < 0) max_x = 0;	if (max_y < 0) max_y = 0;	if (new_x < 0) new_x = 0;	if (new_y < 0) new_y = 0;	if (new_x > max_x) new_x = max_x;	if (new_y > max_y) new_y = max_y;	if (new_x == gw->scroll_x && new_y == gw->scroll_y) return;	gw->scroll_x = new_x;	gw->scroll_y = new_y;	if (gw->vscroll != NULL) SetControlValue(gw->vscroll, (short)new_y);	if (gw->hscroll != NULL) SetControlValue(gw->hscroll, (short)new_x);	macos9_window_invalidate_content(gw);}voidmacos9_window_scroll_by(struct gui_window *gw, int dx, int dy){	macos9_window_scroll_to(gw, gw->scroll_x + dx, gw->scroll_y + dy);}/* ---- Scroll bar click ---- *//* fixes147 -- no action UPP. TrackControl is called with NULL so * CarbonLib never dispatches through a UPP. The CDEF * (kControlScrollBarLiveProc = 386) live-tracks the thumb while * the mouse is held, so GetControlValue on return holds the final * position for thumb drags. Arrow and page clicks scroll by one * step per click, driven by the ControlPartCode that FindControl * returned in the caller. Trade-off: no auto-repeat on hold; the * keyboard arrow-key path (autoKey) still repeats as before. * * fixes149 -- defensive port-set at entry + heavy file-channel * instrumentation before every call that could dispatch through * a garbage function pointer. Log lines are the post-crash * back trace: if sb_call_track appears but sb_post_track does * not, TrackControl itself crashes. If sb_ctrl_state shows * owner != gw_window, the ControlRef is orphaned. */voidmacos9_window_handle_scrollbar_click(struct gui_window *gw,		ControlRef ctrl, short cpart, void *local_pt){#ifdef __MACOS9__	Point pt;	int is_vertical;	int view_w, view_h, page_v, page_h;	int delta_x, delta_y;	short thumb_val;	short ctrl_val, ctrl_min, ctrl_max;	WindowRef ctrl_owner;	short ctrl_vis;	if (gw == NULL || ctrl == NULL || local_pt == NULL) {		macsurf_debug_log_writef("sb_handler null-arg gw=%p ctrl=%p lp=%p",			gw, ctrl, local_pt);		return;	}	/* fixes149 -- defensive port set before any Control Manager	 * call. TrackControl and the live-tracking CDEF paint/measure	 * via the current port; if the caller left the port as	 * something other than gw->window, the CDEF can dispatch	 * through a garbage grafPort function pointer. Zero cost;	 * covers crash-hypothesis 3 from the fixes149 audit. */	if (gw->window != NULL) {		SetPortWindowPort(gw->window);	}	macsurf_debug_log_writef(		"sb_handler gw=%p ctrl=%p cpart=%d vscroll=%p hscroll=%p",		gw, ctrl, (int)cpart, gw->vscroll, gw->hscroll);	/* Deref the ControlRef internals for the log. If ctrl is	 * stale (passed null-check but points to freed memory),	 * this line crashes and the sb_handler entry above is the	 * last surviving log line -- which tells us the ControlRef	 * itself is the problem, not TrackControl. */	ctrl_owner = (**ctrl).contrlOwner;	ctrl_vis   = (short)(**ctrl).contrlVis;	macsurf_debug_log_writef(		"sb_ctrl_state ctrl=%p owner=%p gw_window=%p visible=%d",		ctrl, ctrl_owner, gw->window, (int)ctrl_vis);	ctrl_val = GetControlValue(ctrl);	ctrl_min = GetControlMinimum(ctrl);	ctrl_max = GetControlMaximum(ctrl);	macsurf_debug_log_writef("sb_pre_track val=%d min=%d max=%d",		(int)ctrl_val, (int)ctrl_min, (int)ctrl_max);	pt = *(Point *)local_pt;	/* Track the click. No action proc -- the CDEF handles visual	 * feedback on its own; we react on return.	 * The sb_call_track / sb_post_track pair lets the post-crash	 * log prove whether TrackControl itself crashes (pre logged,	 * post absent) or whether the crash is downstream of its	 * return (both logged, later entry missing). */	macsurf_debug_log_writef("sb_call_track ctrl=%p cpart=%d upp=%p",		ctrl, (int)cpart, (void *)0);	(void)TrackControl(ctrl, pt, NULL);	macsurf_debug_log_writef("sb_post_track newval=%d",		(int)GetControlValue(ctrl));	is_vertical = (ctrl == gw->vscroll);	macsurf_debug_log_writef("sb_dispatch cpart=%d vert=%d",		(int)cpart, is_vertical);	if (cpart == kControlIndicatorPart) {		/* Thumb drag -- live-tracking CDEF left final value in the		 * control. Pull it and scroll_to. */		thumb_val = GetControlValue(ctrl);		if (is_vertical) {			macos9_window_scroll_to(gw, gw->scroll_x, (int)thumb_val);		} else {			macos9_window_scroll_to(gw, (int)thumb_val, gw->scroll_y);		}		return;	}	/* Arrow or page part -- apply a single step in the right axis. */	view_w = content_view_width(gw);	view_h = content_view_height(gw);	page_v = view_h - 32; if (page_v < 16) page_v = 16;	page_h = view_w - 32; if (page_h < 16) page_h = 16;	delta_x = 0;	delta_y = 0;	if (is_vertical) {		switch (cpart) {		case kControlUpButtonPart:    delta_y = -MACOS9_KEY_SCROLL_STEP; break;		case kControlDownButtonPart:  delta_y =  MACOS9_KEY_SCROLL_STEP; break;		case kControlPageUpPart:      delta_y = -page_v; break;		case kControlPageDownPart:    delta_y =  page_v; break;		default: return;		}	} else {		switch (cpart) {		case kControlUpButtonPart:    delta_x = -MACOS9_KEY_SCROLL_STEP; break;		case kControlDownButtonPart:  delta_x =  MACOS9_KEY_SCROLL_STEP; break;		case kControlPageUpPart:      delta_x = -page_h; break;		case kControlPageDownPart:    delta_x =  page_h; break;		default: return;		}	}	macos9_window_scroll_by(gw, delta_x, delta_y);#else	(void)gw; (void)ctrl; (void)cpart; (void)local_pt;#endif}/* ---- TextEdit URL field activation ---- */voidmacos9_window_te_activate_url(struct gui_window *gw){#ifdef __MACOS9__	macsurf_debug_log_writef(		"te_activate gw=%p url_te=%p field_active=%d",		gw, gw ? gw->url_te : NULL,		gw ? (int)gw->url_field_active : -1);	if (gw == NULL || gw->url_te == NULL) return;	if (gw->url_field_active) return;	SetPortWindowPort(gw->window);	TEActivate(gw->url_te);	gw->url_field_active = true;	macos9_window_invalidate_url(gw);	macsurf_debug_log_write("te_activate done");#else	(void)gw;#endif}voidmacos9_window_te_deactivate_url(struct gui_window *gw){#ifdef __MACOS9__	if (gw == NULL || gw->url_te == NULL) return;	if (!gw->url_field_active) return;	SetPortWindowPort(gw->window);	TEDeactivate(gw->url_te);	gw->url_field_active = false;	macos9_window_invalidate_url(gw);#else	(void)gw;#endif}/* ---- Navigation ---- */static voidset_url_te_text(struct gui_window *gw, const char *url){	if (gw == NULL || gw->url_te == NULL || url == NULL) return;	SetPortWindowPort(gw->window);	TESetText(url, (long)strlen(url), gw->url_te);	macos9_window_invalidate_url(gw);}voidmacos9_window_navigate(struct gui_window *gw, const char *url){	nsurl *nsu;	nserror err;	if (gw == NULL || url == NULL || url[0] == '\0') return;	set_url_te_text(gw, url);	set_status_text(gw, "Loading...");	macos9_window_invalidate_status(gw);	if (gw->bw == NULL) return;	err = nsurl_create(url, &nsu);	if (err != NSERROR_OK) {		MS_LOG("nav: nsurl_create fail");		return;	}	err = browser_window_navigate(gw->bw, nsu, NULL,			BW_NAVIGATE_HISTORY, NULL, NULL, NULL);	nsurl_unref(nsu);	if (err != NSERROR_OK) {		MS_LOG("nav: navigate fail");	}	macos9_window_update_button_states(gw);}voidmacos9_window_address_bar_submit(struct gui_window *gw){	CharsHandle h;	long len;	char raw[MACSURF_URL_MAX];	char final_url[MACSURF_URL_MAX + 8];	size_t off;	long end;	if (gw == NULL || gw->url_te == NULL) return;	h = TEGetText(gw->url_te);	if (h == NULL) {		MS_LOG("submit: TEGetText NULL");		return;	}	len = GetHandleSize((Handle)h);	if (len <= 0) {		MS_LOG("submit: empty");		return;	}	if (len >= (long)sizeof(raw)) len = (long)sizeof(raw) - 1;	HLock((Handle)h);	memcpy(raw, *(char **)h, (size_t)len);	HUnlock((Handle)h);	raw[len] = '\0';	off = 0;	while (off < (size_t)len &&	       (raw[off] == ' ' || raw[off] == '\t' ||	        raw[off] == '\r' || raw[off] == '\n')) {		off++;	}	end = len;	while (end > (long)off &&	       (raw[end - 1] == ' ' || raw[end - 1] == '\t' ||	        raw[end - 1] == '\r' || raw[end - 1] == '\n')) {		end--;	}	raw[end] = '\0';	if (end == (long)off) {		MS_LOG("submit: whitespace only");		return;	}	if (strstr(raw + off, "://") == NULL) {		strcpy(final_url, "http://");		strncat(final_url, raw + off, sizeof(final_url) - 8);	} else {		strncpy(final_url, raw + off, sizeof(final_url) - 1);		final_url[sizeof(final_url) - 1] = '\0';	}	MS_LOG(final_url);	macos9_window_te_deactivate_url(gw);	macos9_window_navigate(gw, final_url);}voidmacos9_window_back(struct gui_window *gw){	if (gw == NULL || gw->bw == NULL) return;	if (browser_window_history_back_available(gw->bw)) {		browser_window_history_back(gw->bw, false);		macos9_window_update_button_states(gw);	}}voidmacos9_window_forward(struct gui_window *gw){	if (gw == NULL || gw->bw == NULL) return;	if (browser_window_history_forward_available(gw->bw)) {		browser_window_history_forward(gw->bw, false);		macos9_window_update_button_states(gw);	}}voidmacos9_window_reload(struct gui_window *gw){	if (gw == NULL || gw->bw == NULL) return;	if (!browser_window_has_content(gw->bw)) return;	browser_window_reload(gw->bw, true);}voidmacos9_window_home(struct gui_window *gw){	macos9_window_navigate(gw, MACSURF_HOME_URL);}voidmacos9_window_update_button_states(struct gui_window *gw){	short back_h, fwd_h, reload_h;	if (gw == NULL) return;	if (gw->bw != NULL) {		back_h   = browser_window_history_back_available(gw->bw)    ? (short)0 : (short)255;		fwd_h    = browser_window_history_forward_available(gw->bw) ? (short)0 : (short)255;		reload_h = browser_window_has_content(gw->bw)               ? (short)0 : (short)255;	} else {		back_h   = 255;		fwd_h    = 255;		reload_h = 255;	}	if (gw->back_btn    != NULL) HiliteControl(gw->back_btn,    back_h);	if (gw->forward_btn != NULL) HiliteControl(gw->forward_btn, fwd_h);	if (gw->reload_btn  != NULL) HiliteControl(gw->reload_btn,  reload_h);	if (gw->home_btn    != NULL) HiliteControl(gw->home_btn,    0);}/* ---- Resize ---- */voidmacos9_window_resize(struct gui_window *gw){#ifdef __MACOS9__	Rect te_rect;	struct TERec **th;#endif	if (gw == NULL) return;	MS_LOG("resize start");	macos9_window_layout(gw);#ifdef __MACOS9__	if (gw->url_te != NULL) {		compute_url_te_rect(&gw->url_rect, &te_rect);		th = (struct TERec **)gw->url_te;		HLock((Handle)gw->url_te);		(**th).destRect = te_rect;		(**th).viewRect = te_rect;		HUnlock((Handle)gw->url_te);		TECalText(gw->url_te);	}#endif	macos9_window_update_scrollbars(gw);	/* Deferred — do NOT call browser_window_schedule_reformat	 * synchronously. It can re-enter layout. */	gw->needs_reformat = true;	macos9_window_invalidate_all(gw);	MS_LOG("resize done");}/* ---- Poll hooks called from the event loop ---- */voidmacos9_windows_te_idle(void){#ifdef __MACOS9__	struct gui_window *gw;	GrafPtr old_port = NULL;	int saved_port = 0;	for (gw = window_list; gw != NULL; gw = gw->next) {		if (gw->url_field_active && gw->url_te != NULL &&		    gw->window != NULL) {			if (!saved_port) {				GetPort(&old_port);				saved_port = 1;			}			SetPortWindowPort(gw->window);			TEIdle(gw->url_te);		}	}	if (saved_port) SetPort(old_port);#endif}voidmacos9_windows_process_deferred(void){	struct gui_window *gw;	for (gw = window_list; gw != NULL; gw = gw->next) {		if (!gw->needs_reformat) continue;		if (gw->bw == NULL) {			gw->needs_reformat = false;			continue;		}		if (gw->reformat_in_progress) {			MS_LOG("reformat reentry blocked");			continue;		}		gw->reformat_in_progress = true;		gw->needs_reformat = false;		MS_LOG("resize reformat");		(void)browser_window_schedule_reformat(gw->bw);		gw->reformat_in_progress = false;		MS_LOG("resize reformat done");	}}/* ---- Window creation ---- */static struct gui_window *macos9_window_create(struct browser_window *bw,		struct gui_window *existing,		gui_window_create_flags flags){	struct gui_window *gw;	Rect bounds;	Rect btn_rect;	Rect sb_rect;	Rect te_rect;	OSStatus err;	short x;	(void)existing;	(void)flags;	gw = calloc(1, sizeof(*gw));	if (gw == NULL) return NULL;	gw->bw = bw;#ifdef __MACOS9__	SetRect(&bounds, 40, 50, 640, 470);#else	bounds.left = 40; bounds.top = 50;	bounds.right = 640; bounds.bottom = 470;#endif	err = CreateNewWindow(kDocumentWindowClass,			kWindowStandardDocumentAttributes,			&bounds, &gw->window);	if (err != noErr) {		free(gw);		return NULL;	}#ifdef __MACOS9__	SetWTitle(gw->window, "\pMacSurf");	/* SetWRefCon before TENew is non-negotiable — TextEdit	 * dereferences a garbage WRefCon on a fresh window and	 * crashes with dsMemWZErr. See CLAUDE.md. */	SetWRefCon(gw->window, (long)gw);	SetPortWindowPort(gw->window);#else	SetWTitle(gw->window, PSTR("MacSurf"));#endif	x = MACOS9_BTN_GAP;	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->back_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pBack",#else			PSTR("Back"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	x = (short)(x + MACOS9_BTN_WIDTH + MACOS9_BTN_GAP);	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->forward_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pFwd",#else			PSTR("Fwd"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	x = (short)(x + MACOS9_BTN_WIDTH + MACOS9_BTN_GAP);	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->reload_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pReload",#else			PSTR("Reload"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	x = (short)(x + MACOS9_BTN_WIDTH + MACOS9_BTN_GAP);	SetRect(&btn_rect, x, MACOS9_BTN_Y,		(short)(x + MACOS9_BTN_WIDTH),		(short)(MACOS9_BTN_Y + MACOS9_BTN_HEIGHT));	gw->home_btn = NewControl(gw->window, &btn_rect,#ifdef __MACOS9__			"\pHome",#else			PSTR("Home"),#endif			true, 0, 0, 1, kControlPushButtonProc, (long)gw);	SetRect(&sb_rect,		(short)(bounds.right - bounds.left - MACOS9_SCROLLBAR_WIDTH),		(short)(MACOS9_TOOLBAR_HEIGHT - 1),		(short)(bounds.right - bounds.left + 1),		(short)(bounds.bottom - bounds.top			- MACOS9_SCROLLBAR_WIDTH - MACOS9_STATUS_HEIGHT + 2));	gw->vscroll = NewControl(gw->window, &sb_rect,#ifdef __MACOS9__			"\p",#else			PSTR(""),#endif			true, 0, 0, 0, kControlScrollBarLiveProc, (long)gw);	SetRect(&sb_rect,		-1,		(short)(bounds.bottom - bounds.top - MACOS9_SCROLLBAR_WIDTH),		(short)(bounds.right - bounds.left			- MACOS9_SCROLLBAR_WIDTH + 1),		(short)(bounds.bottom - bounds.top + 1));	gw->hscroll = NewControl(gw->window, &sb_rect,#ifdef __MACOS9__			"\p",#else			PSTR(""),#endif			true, 0, 0, 0, kControlScrollBarLiveProc, (long)gw);	/* Compute rects first so TENew gets the right destRect / viewRect. */	macos9_window_layout(gw);#ifdef __MACOS9__	/* Font/size captured by TENew from the port. */	TextFont(kFontIDMonaco);	TextSize(10);	TextFace(0);	compute_url_te_rect(&gw->url_rect, &te_rect);	gw->url_te = TENew(&te_rect, &te_rect);	if (gw->url_te != NULL) {		const char *home = MACSURF_HOME_URL;		TESetText(home, (long)strlen(home), gw->url_te);	}#else	(void)te_rect;	gw->url_te = NULL;#endif	gw->url_field_active = false;	gw->scroll_x = 0;	gw->scroll_y = 0;	gw->content_width = 0;	gw->content_height = 0;	gw->needs_reformat = false;	gw->reformat_in_progress = false;	set_status_text(gw, "Ready");	macos9_window_update_scrollbars(gw);	macos9_window_update_button_states(gw);	ShowWindow(gw->window);	gw->next = window_list;	window_list = gw;	return gw;}voidmacos9_window_destroy(struct gui_window *gw){	struct gui_window **p;	for (p = &window_list; *p != NULL; p = &(*p)->next) {		if (*p == gw) {			*p = gw->next;			break;		}	}#ifdef __MACOS9__	if (gw->url_te != NULL) {		TEDispose(gw->url_te);		gw->url_te = NULL;	}	/* fixes146 -- null control refs before DisposeWindow.	 * DisposeWindow auto-disposes owned controls; anything	 * that might still read gw during that sequence sees NULL	 * rather than stale handles. Belt-and-suspenders for	 * wheel-crash hypothesis where Control Manager state	 * gets dereferenced through a dangling handle. */	gw->back_btn = NULL;	gw->forward_btn = NULL;	gw->reload_btn = NULL;	gw->home_btn = NULL;	gw->vscroll = NULL;	gw->hscroll = NULL;#endif	if (gw->window != NULL) {		DisposeWindow(gw->window);		gw->window = NULL;	}	free(gw);}/* ---- gui_window_table callbacks ---- */static nserrormacos9_gw_invalidate(struct gui_window *gw, const struct rect *rect){	(void)rect;	MS_LOG("gui inv");	macos9_window_invalidate_content(gw);	return NSERROR_OK;}static boolmacos9_gw_get_scroll(struct gui_window *gw, int *sx, int *sy){	if (sx) *sx = gw->scroll_x;	if (sy) *sy = gw->scroll_y;	return true;}static nserrormacos9_gw_set_scroll(struct gui_window *gw, const struct rect *rect){	if (rect != NULL) {		macos9_window_scroll_to(gw, rect->x0, rect->y0);	}	return NSERROR_OK;}static nserrormacos9_gw_get_dimensions(struct gui_window *gw, int *width, int *height){	if (width)  *width  = gw->content_rect.right - gw->content_rect.left;	if (height) *height = gw->content_rect.bottom - gw->content_rect.top;	return NSERROR_OK;}static nserrormacos9_gw_event(struct gui_window *gw, enum gui_window_event event){	switch (event) {	case GW_EVENT_UPDATE_EXTENT:	case GW_EVENT_NEW_CONTENT:		macos9_window_update_extents(gw);		macos9_window_update_button_states(gw);		macos9_window_invalidate_all(gw);		break;	case GW_EVENT_STOP_THROBBER:		set_status_text(gw, "Done");		macos9_window_invalidate_status(gw);		break;	case GW_EVENT_START_THROBBER:		set_status_text(gw, "Loading...");		macos9_window_invalidate_status(gw);		break;	default:		break;	}	return NSERROR_OK;}static voidmacos9_gw_set_title(struct gui_window *gw, const char *title){	Str255 ptitle;	if (gw == NULL || gw->window == NULL || title == NULL) return;#ifdef MACSURF_DEBUG	if (macsurf_debug_title_is_locked()) return;#endif	set_pstring(ptitle, title);	SetWTitle(gw->window, ptitle);}static nserrormacos9_gw_set_url(struct gui_window *gw, struct nsurl *url){	const char *u;	if (gw == NULL || url == NULL) return NSERROR_OK;	u = nsurl_access(url);	if (u == NULL) return NSERROR_OK;	set_url_te_text(gw, u);	return NSERROR_OK;}static voidmacos9_gw_set_icon(struct gui_window *g, struct hlcache_handle *i){ (void)g; (void)i; }static voidmacos9_gw_set_status(struct gui_window *g, const char *text){	if (g == NULL || text == NULL) return;	set_status_text(g, text);	macos9_window_invalidate_status(g);}static voidmacos9_gw_set_pointer(struct gui_window *g, enum gui_pointer_shape s){ (void)g; (void)s; }static voidmacos9_gw_place_caret(struct gui_window *g, int x, int y, int h,		const struct rect *clip){ (void)g;(void)x;(void)y;(void)h;(void)clip; }static boolmacos9_gw_drag_start(struct gui_window *g, gui_drag_type t,		const struct rect *r){ (void)g;(void)t;(void)r; return false; }static nserrormacos9_gw_save_link(struct gui_window *g, struct nsurl *u, const char *t){ (void)g;(void)u;(void)t; return NSERROR_OK; }static voidmacos9_gw_create_form_select_menu(struct gui_window *g,		struct form_control *c){ (void)g;(void)c; }static voidmacos9_gw_file_gadget_open(struct gui_window *g,		struct hlcache_handle *h, struct form_control *f){ (void)g;(void)h;(void)f; }static voidmacos9_gw_drag_save_object(struct gui_window *g,		struct hlcache_handle *c, gui_save_type t){ (void)g;(void)c;(void)t; }static voidmacos9_gw_drag_save_selection(struct gui_window *g, const char *s){ (void)g;(void)s; }static voidmacos9_gw_console_log(struct gui_window *g,		browser_window_console_source src, const char *m, size_t l,		browser_window_console_flags f){ (void)g;(void)src;(void)m;(void)l;(void)f; }struct gui_window *macos9_create_initial_window(void){	return macos9_window_create(NULL, NULL, 0);}static struct gui_window_table window_table = {	macos9_window_create,	macos9_window_destroy,	macos9_gw_invalidate,	(unsigned char (*)(struct gui_window *, int *, int *))		macos9_gw_get_scroll,	macos9_gw_set_scroll,	macos9_gw_get_dimensions,	macos9_gw_event,	macos9_gw_set_title,	macos9_gw_set_url,	macos9_gw_set_icon,	macos9_gw_set_status,	macos9_gw_set_pointer,	macos9_gw_place_caret,	(unsigned char (*)(struct gui_window *, gui_drag_type, const struct rect *))		macos9_gw_drag_start,	macos9_gw_save_link,	macos9_gw_create_form_select_menu,	macos9_gw_file_gadget_open,	macos9_gw_drag_save_object,	macos9_gw_drag_save_selection,	macos9_gw_console_log};struct gui_window_table *macos9_window_table = &window_table;