/*
 * MacSurf -- macsurf_js_dom_stubs.c
 *
 * All stubs in this file have been replaced by real libdom symbols
 * since the dom_parser.c port landed. The file is kept in the project
 * list as a no-op so the .mcp doesn't need to be re-edited.
 * Safe to remove from the project list.
 */
static int macsurf_js_dom_stubs_placeholder_;
